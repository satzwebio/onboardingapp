apiVersion: v1
kind: ConfigMap
metadata:
  name: team-alpha-config
  namespace: team-alpha-dev
data:
  APP_CONFIG: |
    environment=development
    log_level=debug
  DB_HOST: team-alpha-db
  DB_PORT: "5432"
  DB_NAME: onboarding